# NAS 设备与运维记录

整理日期：2026-09-10。合并搭建进度、运维记录及 Docker/NPM 部署证据。下面均为原记录中的设备信息和操作结果，本次未连接 NAS 复验。网络目标及待验收清单只在 [网络安全架构](../architecture/家庭NAS网络安全架构.md) 维护。

## 设备与存储

| 项目 | 最近资料中的记录 |
| --- | --- |
| 主机 | `ynas`，TerraMaster F4-424 Pro，Intel N305，32 GB DDR5 |
| 系统 | Debian 13.4，安装在 NVMe；旧 Debian 12 装机计划已废弃 |
| 用户与权限 | `yao` UID 1000；`nas-data` GID 1001；曾加入 sudo / docker，当前权限需复核 |
| 核显 | `/dev/dri/renderD128`，render GID 992；媒体服务使用 Intel QSV / VAAPI |
| 网络 | NAS `192.168.50.33`，网关 `192.168.50.1`，RT-AX86U Pro（koolshare） |
| 光猫 | 中兴 F657GV；历史记录已改桥接，AX86U PPPoE 拨号，IPv6 Native |
| 配置位置 | `/opt/nas`，独立私库 `jds5/nas-config`；生产 Compose 与维护脚本未收录于本项目 |
| 温度与健康 | IT8613E 传感器曾使用 DKMS 驱动；smartmontools、nvme-cli、Beszel 等用于监控 |

| 历史设备名 | 容量 | 挂载点 | 用途 |
| --- | --- | --- | --- |
| NVMe | 2 T | `/opt/nas` | 系统、配置、应用数据库与下载缓存 |
| `sdc` | 16 T | `/mnt/pool-main` | 主存储，保持常转，不纳入备份盘休眠 |
| `sdb` | 2 T | `/mnt/critical-mirror` | restic 仓库 |
| `sda` | 3 T | `/mnt/secondary-mirror` | 镜像副本 |

盘符可能随重启改变；稳定 by-id 与休眠操作边界见 [备份盘休眠](备份盘休眠.md)。早期记录另有一块重映射异常的 3T 盘留在 PC、未接入 NAS，不能与当前 Toshiba 备份盘混淆。4G eMMC 的旧系统残留保留未动，启动项使用 NVMe；本次不提供重新装机、格式化或清残留指令。

## 已记录的变更与冲突处理

| 日期 | 原记录中的结果 | 当前解释 |
| --- | --- | --- |
| 2026-06 | restic 扩展覆盖 `/opt/nas`，镜像及 OneDrive 跟随复制 | 取代装机进度中的“备份未部署”；恢复演练仍缺证据 |
| 2026-06-17 | 自纠正 cron 接替 hd-idle，短阈值及 passthrough 唤醒后复睡测试通过 | 取代“最终方案 hd-idle”，后续长期监控尚缺 |
| 2026-08-23 | NPM `origin-home` 回源测试 200；没有 Worker Secret 也能直接读取 | 源站认证缺口没有后续关闭证据 |
| 2026-08-30 | Homeland 已恢复自选 Cloudflare IP 入口；公开 200、管理 Access 302、匿名写 403 | 取代更早的“转发关闭”；本次未复测在线状态 |
| 2026-08-31 | 真实 Writer DELETE 200，数据库目标记录消失 | 取代“合法 Writer 从未验收”；不等于完整测试矩阵已通过 |
| 2026-09-01 | 媒体服务版本核验与升级 | 见下表，版本不代表今日最新 |
| 2026-09-10 原评审 | Homeland Compose 仍使用 `common_network` / `nas-net`，无宿主机端口 | 独立 front/back 网络目标尚未闭环 |

## NPM、DDNS 与 Homeland

- NPM 位于 `/opt/nas/npm`，历史端口映射为 `8443:443` 与 `192.168.50.33:81:81`。同网容器仍可访问 `npm:81`，用户此前接受此风险；不要据 LAN 绑定宣称完全隔离。
- Homeland 上游为 `http://homeland_frontend:3000`，basePath `/home`，反代保留 URI 和 `Cf-Access-Jwt-Assertion`。源码与 Compose 仍由 [Homeland](https://github.com/jds5/homeland) 维护。
- 根域 `rokano.org` 使用单独证书，`*.rokano.org` 不覆盖根域；`origin-home.rokano.org` 使用通配符证书。DNS Challenge 与续期由 NPM 管理。
- 2026-08-23 曾备份 NPM 数据库到 `/data/database.sqlite.backup-20260823-origin-home`；文件是否仍在需现场核对，不能代替新变更前备份。
- `ddns-go` 位于 `/opt/nas/ddns-go`；旧记录曾把根域与四个媒体子域一起更新到家庭 IP，但根域后来改为自选 Cloudflare IP。后续必须核对实际 DDNS 管理列表，避免旧任务把优选入口覆盖成家庭 IP。旧公网 IP 和 DNS 表已从维护文档删除。

### 2026-08-31 写入 403 修复

`origin-home` Proxy Host 7 的 `/home` 曾存在 `limit_except GET HEAD OPTIONS { deny all; }`，导致合法 Writer 在 NPM 层被拒绝，应用收不到请求。原记录通过 NPM API 只移除了 `/home` 的该限制，保留 assertion 透传，未改 Stock 的只读规则。

执行 `nginx -t`、重启 NPM 后，匿名 POST 仍由应用拒绝为 403；真实 Access Writer DELETE 返回 200，数据库目标记录消失。后续不能再对 Homeland 的整个 `/home` 无条件禁写，管理区写入由应用 JWT + CSRF 校验。源站公开读的防绕过仍须单独补齐。

### 公网故障时临时阅读

从可信 LAN 或已验证的 VPN 建立 SSH 隧道。容器 IP 会随重建改变，先在 NAS 查询 `homeland_frontend` 对应网络的实际地址，再替换下面占位符；不要复制旧文档中的固定容器 IP。

```bash
ssh -N -L 127.0.0.1:33000:<容器IP>:3000 yao@192.168.50.33
```

浏览器访问 `http://127.0.0.1:33000/home/docs`；这里只提供临时阅读，不修改 Access 配置或创建公网入口。

## 备份与恢复

原记录中的分工是 Git 管配置文本，restic 管应用数据与媒体备份；Git 不纳入数据库、自动 backup、缓存或凭据。

```text
restic-backup [02:00] → critical-mirror
                         ├─ restic-mirror [06:00] → secondary-mirror
                         └─ restic-onedrive [07:00] → OneDrive
git-backup [04:00] → GitHub nas-config
```

`restic-backup.sh` 已记录加入 `/opt/nas`，排除 `/opt/nas/cache`、`/opt/nas/immich/ml-cache`、`/opt/nas/.git`。镜像和 OneDrive 任务复制整个仓库，随扩展跟进。脚本未在本仓库，应从受控配置源核对，不根据文档重建生产脚本。

历史故障是应用自动备份进入 Git 导致膨胀、推送失败；当时已清理 Git 历史。这里不保留 `filter-repo --force` 或强推操作配方，正常维护应先修正忽略规则，数据由备份系统保护。

待执行一次隔离恢复：保存所需数据库一致性备份、配置、证书与密钥恢复材料，分别恢复 Homeland SQLite、NPM、Immich 数据库与照片，记录快照时间、恢复步骤、服务验证和回滚结果。现有同步成功记录不能替代恢复成功证据。

## 排障与监控遗留

- Tailscale DNS：历史容器保留 `100.100.100.100` 后发生域名解析失败。调整 DNS 接管后逐个核对受影响容器的 `resolv.conf` 与外部解析，按需重建，避免不加区分重建全部生产服务。
- Sonarr 报错需读 Sonarr 本身日志，不能只根据 Seerr 转述的 503 判断；历史故障涉及 `skyhook.sonarr.tv` 解析。
- Uptime Kuma 曾有 `socket.getPeerCertificate is not a function`；备份 Push 重试曾调整为 2、间隔 3600 秒，恢复情况与心跳是否正常仍需核验。
- 旧 Uptime Kuma Push Token 曾写入 Git 历史；当前文档脱敏不代表完成轮换。需要验证旧值失效及新心跳有效，不记录任何真实值。
- diun / uptime-kuma / dozzle / beszel-agent 曾挂载 Docker socket，权限收窄待核验。
- HA 留作家装后的事项，尚不按已部署服务维护。基础控制应避免依赖 NAS 单点；需要时另做实施方案。

## 媒体服务最后记录（2026-09-01）

> **目的**：在计划开放 Immich、Jellyfin、Navidrome 直连入口前，核对并更新应用镜像，避免把已知旧版本直接暴露到公网。官方版本依据均于 2026-09-01 查询项目 GitHub Releases。

| 服务 | 更新前 | 当日查询版本 | 执行动作 | 更新后实测 |
|---|---:|---:|---|---|
| Jellyfin | 10.11.8 | 10.11.11 | 拉取 `jellyfin/jellyfin:latest` 并由 Compose 重建 | 容器命令报告 `10.11.11.0`；Docker health 为 healthy；`/health` 返回 `Healthy`；`/System/Info/Public` 返回版本 10.11.11；启动后未观察到 error/fatal/fail/exception |
| Navidrome | 0.61.2 | 0.63.2 | 拉取 `deluan/navidrome:latest` 并由 Compose 重建 | 容器命令报告 `0.63.2 (be10f89c)`；内部首页和 NPM HTTPS 均返回预期登录重定向 302；启动后未观察到 error/fatal/panic/fail |
| Immich | v3.1.0 | v3.1.0 | 无版本差异，未重建数据库或应用容器 | Server 与 Machine Learning 镜像标签均为 v3.1.0；容器健康；`/api/server/ping` 返回 `pong`；NPM HTTPS 返回 200 |

官方来源：[Jellyfin Releases](https://github.com/jellyfin/jellyfin/releases) · [Navidrome Releases](https://github.com/navidrome/navidrome/releases) · [Immich Releases](https://github.com/immich-app/immich/releases)（访问日期：2026-09-01）。

遗留与边界：

1. Immich 每日缩略图任务仍对同一视频资产报告 `Missing video metadata`；这是升级前已存在的数据/媒体任务问题，本次没有据此宣称 Immich 完全无错误。
2. `music.rokano.org:443` 当前从 NAS 无法连接；本机经 NPM `:8443` 的 HTTPS 请求为 302。标准 443 直连尚未上线，不能把 8443 本机 smoke test写成公网直连已完成。
3. Jellyfin Compose 当前将 `8096/8920 TCP` 与 `1900/7359 UDP` 发布到宿主机所有地址，和“只经 NPM、不发布 8096”的目标不一致。开放公网直连前应移除不需要的 TCP 发布；DLNA/发现 UDP 是否保留须按 LAN 功能单独决定，并从外网复扫确认路由器只开放预期的 8443。

## 后续记录格式

每次实际维护追加日期、目标、操作、验证结果、回滚位置和遗留事项；未执行不勾完成。历史原文、迁移与删减范围见 [迁移与清理记录](../migration/README.md)。

## Android 测试包局域网分发（2026-09-16）

本机 `ynas` / `yao` 部署用户服务 `nas-app-download.service`，监听 `192.168.50.33:8765`，来源限制 `192.168.50.0/24`。只读分发 Android 0.1.1 签名测试包；未修改 NPM、路由器转发、生产 SSH 或防火墙。APK 不入 Git，源码与 unit 位于 [distribution](../../apps/android/distribution/README.md)。

验证：Release 构建、Lint、APK 签名校验通过；下载服务 3 项测试通过；实际 HTTP 页面与 APK 返回成功，下载 SHA-256 与本机产物一致，包内 versionCode=2 / versionName=0.1.1。未在手机或外部网络验收。

本机 SSH 的只读核验：对 `yao` 的 LAN 握手仅提供 `publickey`，无密钥登录被拒绝；完整 `sshd -T` 需要管理员输入 sudo 密码。`.ssh` 目录收紧及公网接入步骤见 [外网连接手册](手机外网连接与SSH密钥配置.md)，未自动执行。

回滚下载站：`systemctl --user disable --now nas-app-download.service`。当前用户 `Linger=no`，最后登录会话结束后下载服务可能停止；常驻配置及恢复步骤见分发说明。

## 手机 App 内外网连接验证（2026-09-16）

证据来源：用户真机反馈。此前 App 局域网连接失败由未授予局域网权限引起，授权后连接成功；用户随后确认已成功通过外网连接。记录为基本连接验证通过，不推断全部终端操作或动画性能已验收。

用户说明家庭公网服务经路由器转发 8443 到 NAS，由 NPM 管理 HTTPS；Cloudflare DNS 通过 DDNS-go 更新运营商动态公网 IP。实际用于 App 的公网域名/IP 和外部 SSH 端口未提供，文档中的 22222 仍为示例。本次仅更新记录，没有更改服务或网络配置。

待验证：中文多行粘贴与回车、锁屏与断线后手动重连、Wi-Fi/蜂窝切换、公网 IP 变化后的 DNS 更新和重新连接、发送结果不确定时不重复提交。

## Codex 聊天式控制与安全诊断（2026-09-16）

Android 0.2.0（versionCode=3）默认进入聊天页，展示用户消息与 Codex 公开回复；一次发送输入，支持 `/` 命令、技能选择和 `!` 用户命令结果，原终端模式保留为可选入口。使用现有 SSH，临时 Python 桥接关联原 Codex 进程打开的 rollout；不启动第二执行器，不新增远程控制端口。

验证：14 项 Python 桥接测试、9 项 JVM 测试、3 项下载站测试全部通过；Debug/Release、Lint 和 APK 签名验证通过。Lint 只有已有的 Gradle 版本提示。真实隔离 Codex 0.154.0 验证 `!printf`、`/status` 和中文/长文本输入识别，当前会话只读验证。桥接资源在 Release APK 内与源码一致。没有 Android 设备连接，未声称本版真机体验或动画帧率已验收。

已更新 `nas-app-download.service` 和 [LAN 下载页](http://192.168.50.33:8765/)，页面与实际 APK 的版本和 SHA-256 一致。使用原测试签名，保留本机 0.1.1 APK 和旧 unit。未更改生产 SSH/NPM/网络配置；手机外网仍使用已有 SSH 入口。

下载服务回滚（不改变 App 数据）：

```bash
cp ~/.config/systemd/user/nas-app-download.service.before-0.2.0 ~/.config/systemd/user/nas-app-download.service
systemctl --user daemon-reload
systemctl --user restart nas-app-download.service
```

这会恢复 0.1.1 APK 的分发；手机 APK 降级可能要求卸载并重新导入连接资料，不自动操作手机。0.2.0 使用中也可直接切回终端模式。

安全诊断与修复见 [报告](../security/ANDROID_APP_SECURITY_REVIEW.md)：修正凭据文件有界读取、消息与会话边界及发送确认；记录共享输入的竞争条件、旧 Token 失效证明缺失、公网边界未复验和媒体脚本 dry-run 仍写日志等待办。未将这些既有待办报告为已修复。

## 蜂窝网络 SSH 失败与 0.2.1 诊断版本（2026-09-16）

用户提供入口 `jelly.rokano.org:2257`、用户名 `yao`；0.2.0 在家庭 Wi-Fi 可连接，4G 下域名连接失败，临时改成当时公网 IPv4 `122.96.13.143` 后仍失败。此前“外网连接成功”的反馈不替代本次蜂窝网络复验。

只读排查：Google / Cloudflare DoH 与 NAS 本机解析均得到同一 A 记录，无 AAAA；NAS 访问该公网地址 2257 可收到 OpenSSH banner，观察到的 Ed25519 公钥指纹与本机主机公钥一致。这仅证明 NAS 侧公网地址回环路径，不证明 4G 可达；未修改 DDNS、路由器、生产 SSH 或认证规则，也未确定具体阻断位置。

0.2.1（versionCode=4）增加不含原始异常的连接错误编号，分别标记私钥加载、DNS、系统网络权限、TCP、SSH 握手、主机指纹与公钥认证。SocketFactory 在真实 TCP 连接完成后记录阶段，主机指纹通过后才标记认证阶段；保持原指纹、公钥认证和超时配置，不新增网络预探测或日志上传。

验证与交付：13 项 JVM 测试通过（含错误分类、真实隔离 SSH 的错误口令和拒绝端口）；Release 构建及 Lint 通过。相同测试签名的 APK 经签名、版本和 LAN HTTP 下载内容一致性核对后发布到原下载页。没有手机端网络错误证据，**本版用于定位，不声称修复 4G 连接**；后续需用户安装后反馈具体错误编号。

下载 unit 备份于 `~/.config/systemd/user/nas-app-download.service.before-0.2.1`，旧版 APK 保留。若需恢复 0.2.0 分发：

```bash
cp ~/.config/systemd/user/nas-app-download.service.before-0.2.1 ~/.config/systemd/user/nas-app-download.service
systemctl --user daemon-reload
systemctl --user restart nas-app-download.service
```


### 4G 失败阶段确认（2026-09-16）

用户更新 0.2.1 后反馈 `E_TCP_TIMEOUT`：实际 TCP connect 超时，尚未进入 SSH 握手/主机指纹校验/公钥认证，因此本次失败不应通过更换密钥或放宽认证处理。此前同一 4G 网络的域名和公网 IP 直连都失败，不能仅归因于 DNS。

再次只读核对：A 记录仍为 `122.96.13.143`；NAS 侧访问公网 2257 收到本机 OpenSSH banner，8443 TCP 也可连接，本机 IPv4/IPv6 的 22 仍监听。以上仍只是 NAS 侧回环证据，不能证明手机来源的入站路径或 WAN 防火墙已放行。

用户随后确认：没有 VPN/代理，App 移动数据已允许，4G 下 NAS 网页可打开。路由器为 ASUS RT-AX86U Pro，用户设置外部 2257、内部 22；完整来源限制及 WAN 防火墙尚未直接核验。Windows 按热点对照步骤反馈 `RemoteAddress=122.96.13.143`、`RemotePort=2257`、`TcpTestSucceeded=False`，独立客户端也复现 TCP 失败；Ping 超时不能单独证明主机离线。

下一步需在 NAS 的 `enp1s0` 短时观察 22 端口的 SYN/SYN-ACK/RST，再从 Windows 重试，以区分入站请求未到达与 NAS/回程问题。之所以观察 22 而非 2257，是因为路由器已做目的端口转换。当前用户直接抓包被权限拒绝，`sudo -n` 也要求密码，故未实际抓取；需管理员在 NAS 终端执行，不向助手提供密码。尚未定位丢包位置，未修改网络配置。


### 手机 SYN 已到达 NAS（2026-09-16）

用户在 NAS 抓包期间用手机 App 重试，观察到同一外部来源向 `192.168.50.33:22` 连续重传 SYN，未在 `enp1s0` 看到相应 SYN-ACK 或 RST。此证据说明该次公网请求已经过转发送达 NAS，不能继续将问题仅归因于 App 或入站端口转发未生效。

只读现场核对：SSH 仍监听 IPv4/IPv6 的 22；到该手机公网来源的普通 IPv4 回程路由为 `192.168.50.1 / enp1s0`。UFW 服务 active，`/etc/ufw/ufw.conf` 为 `ENABLED=yes`，配置默认入站策略 `DROP`；firewalld、fail2ban、nftables 服务 inactive（不代表内核没有 nftables 规则）。Tailscale active，所查普通回程没有走 tailscale0。

UFW 入站规则成为重点疑点，但默认 DROP 本身不证明缺少允许规则。`sudo -n ufw status verbose` 仍要求密码，尚未取得生效允许/拒绝列表；已请用户在 NAS 终端提供 `sudo ufw status verbose` 输出。没有关闭防火墙或增加放行规则。后续若确认仅允许 LAN SSH，针对本机 IPv4/接口补齐授权的远程 SSH 规则前需备份，并给出对应回滚。


### 确认 UFW 缺少公网 SSH 允许规则（2026-09-16）

用户提供 `sudo ufw status verbose`：UFW active，默认 deny incoming；SSH 22/tcp 只允许 `192.168.0.0/16`，并有该 LAN 范围的全端口允许规则，未有公网 IPv4 来源访问 22 的允许规则。这与 LAN 成功、4G SYN 到达但不回复的现象一致，已定位阻断原因。

已提供经 `enp1s0` 到 `192.168.50.33:22/tcp` 的任意 IPv4 来源允许规则，适配用户已选择的公网 key-only SSH 路径；包含 `/etc/ufw` 备份、状态检查、4G 验证与单规则删除回滚，详见 [SSH 配置手册](手机外网连接与SSH密钥配置.md)。助手无 sudo 权限，**未执行防火墙修改，尚待用户执行和实测**；未关闭 UFW、未开放 IPv6 SSH或修改其他服务规则。


## 4G SSH 恢复确认与后续体验规划（2026-09-17）

在定位 UFW 仅允许 LAN SSH 并提供指定接口/IPv4/22 的放行与回滚步骤后，用户反馈“可以了”。按用户实机反馈记录为本次蜂窝 SSH 连接恢复；助手没有管理员权限，未独立读取修改后的防火墙规则，不据此认定锁屏、换网、DDNS 更新和全部公网边界均已验收。

用户开始讨论后续 App 功能，建议优先完善发送状态/草稿、重新接回会话、阅读与滚动，再扩展连接诊断和改动审阅。提案及验收边界集中在 [手机接入规划](手机接入现有tmux规划.md)，本轮仅更新文档，未实现或发布新 App 版本。

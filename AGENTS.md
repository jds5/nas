# 协作约定

本项目用于 NAS 维护，以下约定适用于整个仓库。

## 资料组织

- 开始维护前阅读 `README.md` 和相关运维记录；网络与公网入口决策以 `docs/architecture/家庭NAS网络安全架构.md` 为准。
- `docs/operations/` 保存带日期的操作与验证记录，`docs/security/` 保存安全实施手册；过时方案通过 Git 历史追溯，不重复归档。
- 历史文档中的“已完成”不等于当前现场状态；变更前核对设备身份、配置和依赖，不直接执行历史版本中的命令。
- 新增资料按用途归类并更新 README 导航；移动文件时同步修复相对链接。
- 来源与未迁移内容见 `docs/migration/README.md`。Homeland 应用代码仍归原项目，生产配置仍需从 NAS / nas-config 核对，不用历史代码片段冒充可部署配置。
- 实施“公开读、登录写”的实验 Web 项目时，阅读统一手册 `docs/security/CLOUDFLARE_ACCESS_IMPLEMENTATION.md`，按其中适用范围执行鉴权与验收，不另建重复规则文档。

## 工作方式

- 默认使用中文沟通和编写文档。
- 修改前先阅读相关文件，遵循现有结构，保持改动简单且聚焦。
- 不假设设备型号、系统版本、磁盘布局或服务部署方式；以实际配置和用户提供的信息为准。
- 新增脚本应说明用途、依赖、运行方式和影响范围。

## 维护安全

- 优先只读排查，避免影响正在运行的服务。
- 涉及删除数据、格式化磁盘、修改存储池或其他不可逆操作时，必须先明确目标、影响及备份情况，并获得用户授权。
- 修改重要配置前保留备份，并提供可执行的回滚步骤。
- 不将密码、令牌、私钥或含敏感信息的日志写入仓库。

## 验证与交付

- 根据改动执行必要的语法检查或功能验证；纯文档修改检查内容和格式即可。
- 未实际执行的操作或验证必须明确说明，不声称已在 NAS 上验证。
- 完成后简要说明改动、验证结果及需要用户关注的事项。

## Git 提交与推送

- 每次项目文件更新并完成必要验证后，及时执行 `git add`、`git commit` 和 `git push`，无需再次询问；用户明确要求暂不提交或推送时除外。
- 提交前检查差异与暂存内容，只提交本次任务相关改动，不纳入凭据、运行数据或无关改动。
- 提交信息使用简洁中文，说明改动目的；逻辑独立的改动按需拆分提交。
- 推送至当前分支对应的远端，不跳过 Git hooks，不强制推送。
- 提交或推送失败时保留本地成果并明确说明原因，不将本地提交成功报告为远端已同步。
- 推送后确认远端同步状态，并在交付时说明提交及推送结果。
